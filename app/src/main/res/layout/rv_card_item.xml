<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="#00162C">

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/ivCardItem"
        android:layout_width="120dp"
        android:layout_height="180dp"
        android:layout_margin="10dp"
        android:scaleType="centerCrop"
        android:padding="1dp"
        android:cropToPadding="true"
        android:background="@drawable/img_border"
        app:siBorderColor="@color/white"
        app:siBorderType="stroke"
        app:siBorderWidth="3dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/tvCardItem"/>

    <TextView
        android:id="@+id/tvCardItem"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="#95"
        android:textSize="30sp"
        android:textColor="@color/white"
        android:layout_marginBottom="10dp"
        app:layout_constraintStart_toStartOf="@+id/ivCardItem"
        app:layout_constraintEnd_toEndOf="@+id/ivCardItem"
        app:layout_constraintTop_toBottomOf="@+id/ivCardItem"
        app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>